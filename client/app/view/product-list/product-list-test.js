'use strict';

describe('myApp.product-list.js module', function() {

  beforeEach(module('myApp.product-list.js'));

  describe('product-list.js controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('View2Ctrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});